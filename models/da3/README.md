# DA3 Models

Place the DA3 files here before running `scripts/prepare_da3.sh`:

- `DA3METRIC-LARGE.onnx`
- `DA3METRIC-LARGE.fp16-batch1.engine`

TensorRT engines are GPU and TensorRT-version specific and are not committed to Git.
